# compose.yml
# PeerPrep - Docker Compose
# Usage: docker compose up --build

services:

  # ============================================
  # MongoDB
  # ============================================
  mongodb:
    image: mongo:7
    container_name: peerprep-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password123
    volumes:
      - mongodb_data:/data/db
    networks:
      - peerprep-network
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh --quiet
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # ============================================
  # Redis
  # ============================================
  redis:
    image: redis:7-alpine
    container_name: peerprep-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - peerprep-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ============================================
  # User Service (Express.js) — Port 3001
  # ============================================
  user-service:
    build:
      context: ./apps/user-service
      dockerfile: Dockerfile
    container_name: peerprep-user-service
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      PORT: 3001
      ENV: DEV
      DB_LOCAL_URI: mongodb://admin:password123@mongodb:27017/peerprepUserServiceDB?authSource=admin
      DB_CLOUD_URI: mongodb://peerprep-admin:Peerprep2526@ac-ycam9om-shard-00-00.6x2xogl.mongodb.net:27017,ac-ycam9om-shard-00-01.6x2xogl.mongodb.net:27017,ac-ycam9om-shard-00-02.6x2xogl.mongodb.net:27017/peerprepUserService?ssl=true&replicaSet=atlas-prkdof-shard-0&authSource=admin&appName=peerprep-g16
      JWT_SECRET: peerprep-group16-secret-change-this-later
      JWT_REFRESH_SECRET: peerprep-group16-secret-change-this-later-123
      ENCRYPTION_KEY: 039bc11590d23ff311bb9b635de73b9eab6b3163be86f600f4ef07db8dba0d68
      ENCRYPTION_IV: 9ea2efa7a529165a6d5fbd311d2cb518
      ADMIN_EMAIL: admin@example.com
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_URL: redis://redis:6379
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - peerprep-network

  # ============================================
  # Question Service (TypeScript) — Port 3002
  # ============================================
  question-service:
    build:
      context: ./apps/question-service
      dockerfile: Dockerfile
    container_name: peerprep-question-service
    restart: unless-stopped
    ports:
      - "3002:3002"
    environment:
      NODE_ENV: production
      PORT: 3002
      # ⚠️ Teammate: add your env vars here
    depends_on:
      mongodb:
        condition: service_healthy
    networks:
      - peerprep-network

  # ============================================
  # Collaboration Service (TypeScript) — Port 3003
  # ============================================
  collaboration-service:
    build:
      context: ./apps/collaboration-service
      dockerfile: Dockerfile
    container_name: peerprep-collaboration-service
    restart: unless-stopped
    ports:
      - "3003:3003"
    environment:
      NODE_ENV: production
      PORT: 3003
      REDIS_URL: redis://redis:6379
      # ⚠️ Teammate: add your env vars here
    depends_on:
      mongodb:
        condition: service_healthy
    networks:
      - peerprep-network

  # ============================================
  # Matching Service (TypeScript) — Port 3004
  # ============================================
  matching-service:
    build:
      context: ./apps/matching-service
      dockerfile: Dockerfile
    container_name: peerprep-matching-service
    restart: unless-stopped
    ports:
      - "3004:3004"
    environment:
      NODE_ENV: production
      PORT: 3004
      REDIS_URL: redis://redis:6379
      # ⚠️ Teammate: add your env vars here
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - peerprep-network

  # ============================================
  # Frontend (React/Vite) — Port 5173
  # ============================================
  frontend:
    build:
      context: ./apps/frontend
      dockerfile: Dockerfile
    container_name: peerprep-frontend
    restart: unless-stopped
    ports:
      - "5173:5173"
    networks:
      - peerprep-network

volumes:
  mongodb_data:
  redis_data:

networks:
  peerprep-network:
    driver: bridge
